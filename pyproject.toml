[project]
name = "securesight"
version = "0.1.0"
description = "Security Incident Classification and SOC Analytics Platform."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"